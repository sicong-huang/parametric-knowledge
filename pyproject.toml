[project]
name = "parametric-knowledge"
version = "0.1.0"
description = "Eval pipeline: does reasoning/RL improve parametric factual recall"
requires-python = ">=3.10"
dependencies = [
    "vllm>=0.6.0",
    "transformers>=4.46.0",
    "datasets>=3.0.0",
    "huggingface_hub>=0.25.0",
    "openai>=1.0",
    "python-dotenv>=1.2.2",
]

[tool.uv]
package = false
